# Reference Playbooks

One `.md` per reference video you drop in `./references/`. Each captures the breakdown (palette, density, motion easing, depth/DOF, post stack, pacing) + how to replicate it in Hyperframes/Remotion/Unreal. Built by reading extracted frames (viewer seat).
